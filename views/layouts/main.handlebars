<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>FueGoals</title>
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
  <header class="display-flex align-center p-2">
    <h1 id="pagetitle">
      <a href="/">
      🔥FueGoals🔥
      </a>
    </h1>
    <!-- Only display the logout button if the user is logged in -->
    <nav>
       <a class="btn navbtns" href="/">Home</a>
    {{#if logged_in}}
        <a class="btn navbtns" href="/dashboard">Dashboard</a>
      <button class="btn" id="logout">logout</button>
    {{else}}
    <a class="btn navbtns" href="/login">Login</a>
    {{/if}}
    </nav>
  </header>
  <main class="container container-fluid mt-5">
    {{{ body }}}
  </main>

  <footer>
      <div>FueGoals was built by HalfFueled</div>
  </footer>

  <!-- Only include the logout script if the user is logged in -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
